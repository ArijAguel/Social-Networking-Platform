import { OrganizationSectorPipe } from './organization-sector.pipe';

describe('OrganizationSectorPipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizationSectorPipe();
    expect(pipe).toBeTruthy();
  });
});
